const productsMiddlewares = require('./products.middleware');
const salesMiddlewares = require('./sales.middleware');
const salesProductsMiddlewares = require('./salesProducts.middleware');

module.exports = {
  productsMiddlewares,
  salesMiddlewares,
  salesProductsMiddlewares,
};
